<% title @page.title %>
<% meta_keywords @page.meta_keywords %>
<% meta_description @page.meta_description %>
<% nav_bar [['首页', '/'],[@page.title, '/pages/'+ @page.path_name]] %>
<% side_news @page.title %>

<div id="page">
  <div id="col1"><div id="col1_content">
    <%=image_tag asset_path("menu_side1_about.jpg") %>
  </div></div><div id="col3"><div id="col3_content">
      <% case @page.path_name%>
      <% when 'about:address' %>
        <%= @page.body.html_safe unless @page.body.blank?%>
        <iframe src="/home/location" width="100%" height="500px" scrolling="no"></iframe>
      <% else %>

      <% end %>
      <%= @page.body.html_safe unless @page.body.blank?%>
  </div></div>
</div>
